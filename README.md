# placeholder Title

- (simulated) sensors in a factory send data
- data sender POSTs to server
- server calculates "anomaly score" and returns

current endpoints:
GET /status
POST /score (JSON)

diagrams (Mermaid):
- architecture: [documentation/architecture.mermaid]
- sequence: [documentation/sequence.mermaid]
